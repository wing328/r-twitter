#' Twitter API v2
#'
#' Twitter API v2 available endpoints
#'
#' The version of the OpenAPI document: 2.49
#' Generated by: https://openapi-generator.tech
#'

#' @export
ReplySettings <- R6::R6Class(
    "ReplySettings",
    public = list(
        initialize = function(...) {
            local.optional.var <- list(...)
            val <- unlist(local.optional.var)
            enumvec <- .parse_ReplySettings()

            stopifnot(length(val) == 1L)

            if (!val %in% enumvec)
                stop("Use one of the valid values: ",
                    paste0(enumvec, collapse = ", "))
            private$value <- val
        },
        toJSON = function() {
            jsonlite::toJSON(private$value, auto_unbox = TRUE)
        },
        fromJSON = function(ReplySettingsJson) {
            private$value <- jsonlite::fromJSON(ReplySettingsJson,
                simplifyVector = FALSE)
            self
        },
        toJSONString = function() {
            as.character(jsonlite::toJSON(private$value,
                auto_unbox = TRUE))
        },
        fromJSONString = function(ReplySettingsJson) {
            private$value <- jsonlite::fromJSON(ReplySettingsJson,
                simplifyVector = FALSE)
            self
        }
    ),
    private = list(
        value = NULL
    )
)

# add to utils.R
.parse_ReplySettings <- function(vals) {
    res <- gsub("^\\[|\\]$", "",
        "[everyone, mentionedUsers, following, other]"
    )
    unlist(strsplit(res, ", "))
}

