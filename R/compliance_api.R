#' Twitter API v2
#'
#' Twitter API v2 available endpoints
#'
#' The version of the OpenAPI document: 2.49
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title Compliance operations
#' @description twitter.Compliance
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ create_batch_compliance_job } \emph{ Create compliance job }
#' Creates a compliance for the given job type
#'
#' \itemize{
#' \item \emph{ @param } create_compliance_job_request \link{CreateComplianceJobRequest}
#' \item \emph{ @returnType } \link{CreateComplianceJobResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | The request has succeeded.
#'
#' \item return type : CreateComplianceJobResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | The request has failed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ get_batch_compliance_job } \emph{ Get Compliance Job }
#' Returns a single Compliance Job by ID
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } compliance_job_fields Enum < [created_at, download_expires_at, download_url, id, name, resumable, status, type, upload_expires_at, upload_url] >
#' \item \emph{ @returnType } \link{Get2ComplianceJobsIdResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | The request has succeeded.
#'
#' \item return type : Get2ComplianceJobsIdResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | The request has failed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ get_tweets_compliance_stream } \emph{ Tweets Compliance stream }
#' Streams 100% of compliance data for Tweets
#'
#' \itemize{
#' \item \emph{ @param } partition integer
#' \item \emph{ @param } backfill_minutes integer
#' \item \emph{ @param } start_time character
#' \item \emph{ @param } end_time character
#' \item \emph{ @returnType } \link{TweetComplianceStreamResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | The request has succeeded.
#'
#' \item return type : TweetComplianceStreamResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | The request has failed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ get_users_compliance_stream } \emph{ Users Compliance stream }
#' Streams 100% of compliance data for Users
#'
#' \itemize{
#' \item \emph{ @param } partition integer
#' \item \emph{ @param } backfill_minutes integer
#' \item \emph{ @param } start_time character
#' \item \emph{ @param } end_time character
#' \item \emph{ @returnType } \link{UserComplianceStreamResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | The request has succeeded.
#'
#' \item return type : UserComplianceStreamResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | The request has failed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ list_batch_compliance_jobs } \emph{ List Compliance Jobs }
#' Returns recent Compliance Jobs for a given job type and optional job status
#'
#' \itemize{
#' \item \emph{ @param } type Enum < [tweets, users] >
#' \item \emph{ @param } status Enum < [created, in_progress, failed, complete] >
#' \item \emph{ @param } compliance_job_fields Enum < [created_at, download_expires_at, download_url, id, name, resumable, status, type, upload_expires_at, upload_url] >
#' \item \emph{ @returnType } \link{Get2ComplianceJobsResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | The request has succeeded.
#'
#' \item return type : Get2ComplianceJobsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | The request has failed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  create_batch_compliance_job  ####################
#'
#' library(twitter)
#' var.create_compliance_job_request <- CreateComplianceJobRequest$new() # CreateComplianceJobRequest | 
#'
#' #Create compliance job
#' api.instance <- ComplianceApi$new()
#'
#' #Configure HTTP bearer authorization: BearerToken
#' api.instance$api_client$bearer_token <- 'TODO_YOUR_BEARER_TOKEN';
#'
#'result <- tryCatch(
#'             api.instance$create_batch_compliance_job(var.create_compliance_job_request),
#'             ApiException = function(ex) ex
#'          )
#' # In case of error, print the error object
#' if(!is.null(result$ApiException)) {
#'   cat(result$ApiException$toString())
#' } else {
#' # deserialized response object
#' response.object <- result$content
#' # response headers
#' response.headers <- result$response$headers
#' # response status code
#' response.status.code <- result$response$status_code
#' }
#'
#'
#' ####################  get_batch_compliance_job  ####################
#'
#' library(twitter)
#' var.id <- "id_example" # character | The ID of the Compliance Job to retrieve.
#' var.compliance_job_fields <- ["[\"created_at\",\"download_expires_at\",\"download_url\",\"id\",\"name\",\"resumable\",\"status\",\"type\",\"upload_expires_at\",\"upload_url\"]"] # set[character] | A comma separated list of ComplianceJob fields to display.
#'
#' #Get Compliance Job
#' api.instance <- ComplianceApi$new()
#'
#' #Configure HTTP bearer authorization: BearerToken
#' api.instance$api_client$bearer_token <- 'TODO_YOUR_BEARER_TOKEN';
#'
#'result <- tryCatch(
#'             api.instance$get_batch_compliance_job(var.id, compliance_job_fields=var.compliance_job_fields),
#'             ApiException = function(ex) ex
#'          )
#' # In case of error, print the error object
#' if(!is.null(result$ApiException)) {
#'   cat(result$ApiException$toString())
#' } else {
#' # deserialized response object
#' response.object <- result$content
#' # response headers
#' response.headers <- result$response$headers
#' # response status code
#' response.status.code <- result$response$status_code
#' }
#'
#'
#' ####################  get_tweets_compliance_stream  ####################
#'
#' library(twitter)
#' var.partition <- 56 # integer | The partition number.
#' var.backfill_minutes <- 56 # integer | The number of minutes of backfill requested.
#' var.start_time <- "2021-02-01T18:40:40.000Z" # character | YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the Tweet Compliance events will be provided.
#' var.end_time <- "2021-02-14T18:40:40.000Z" # character | YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp to which the Tweet Compliance events will be provided.
#'
#' #Tweets Compliance stream
#' api.instance <- ComplianceApi$new()
#'
#' #Configure HTTP bearer authorization: BearerToken
#' api.instance$api_client$bearer_token <- 'TODO_YOUR_BEARER_TOKEN';
#'
#'result <- tryCatch(
#'             api.instance$get_tweets_compliance_stream(var.partition, backfill_minutes=var.backfill_minutes, start_time=var.start_time, end_time=var.end_time),
#'             ApiException = function(ex) ex
#'          )
#' # In case of error, print the error object
#' if(!is.null(result$ApiException)) {
#'   cat(result$ApiException$toString())
#' } else {
#' # deserialized response object
#' response.object <- result$content
#' # response headers
#' response.headers <- result$response$headers
#' # response status code
#' response.status.code <- result$response$status_code
#' }
#'
#'
#' ####################  get_users_compliance_stream  ####################
#'
#' library(twitter)
#' var.partition <- 56 # integer | The partition number.
#' var.backfill_minutes <- 56 # integer | The number of minutes of backfill requested.
#' var.start_time <- "2021-02-01T18:40:40.000Z" # character | YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the User Compliance events will be provided.
#' var.end_time <- "2021-02-01T18:40:40.000Z" # character | YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp from which the User Compliance events will be provided.
#'
#' #Users Compliance stream
#' api.instance <- ComplianceApi$new()
#'
#' #Configure HTTP bearer authorization: BearerToken
#' api.instance$api_client$bearer_token <- 'TODO_YOUR_BEARER_TOKEN';
#'
#'result <- tryCatch(
#'             api.instance$get_users_compliance_stream(var.partition, backfill_minutes=var.backfill_minutes, start_time=var.start_time, end_time=var.end_time),
#'             ApiException = function(ex) ex
#'          )
#' # In case of error, print the error object
#' if(!is.null(result$ApiException)) {
#'   cat(result$ApiException$toString())
#' } else {
#' # deserialized response object
#' response.object <- result$content
#' # response headers
#' response.headers <- result$response$headers
#' # response status code
#' response.status.code <- result$response$status_code
#' }
#'
#'
#' ####################  list_batch_compliance_jobs  ####################
#'
#' library(twitter)
#' var.type <- "type_example" # character | Type of Compliance Job to list.
#' var.status <- "status_example" # character | Status of Compliance Job to list.
#' var.compliance_job_fields <- ["[\"created_at\",\"download_expires_at\",\"download_url\",\"id\",\"name\",\"resumable\",\"status\",\"type\",\"upload_expires_at\",\"upload_url\"]"] # set[character] | A comma separated list of ComplianceJob fields to display.
#'
#' #List Compliance Jobs
#' api.instance <- ComplianceApi$new()
#'
#' #Configure HTTP bearer authorization: BearerToken
#' api.instance$api_client$bearer_token <- 'TODO_YOUR_BEARER_TOKEN';
#'
#'result <- tryCatch(
#'             api.instance$list_batch_compliance_jobs(var.type, status=var.status, compliance_job_fields=var.compliance_job_fields),
#'             ApiException = function(ex) ex
#'          )
#' # In case of error, print the error object
#' if(!is.null(result$ApiException)) {
#'   cat(result$ApiException$toString())
#' } else {
#' # deserialized response object
#' response.object <- result$content
#' # response headers
#' response.headers <- result$response$headers
#' # response status code
#' response.status.code <- result$response$status_code
#' }
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @importFrom rlang abort
#' @export
ComplianceApi <- R6::R6Class(
  "ComplianceApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ComplianceApi.
    #'
    #' @description
    #' Initialize a new ComplianceApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Create compliance job
    #'
    #' @description
    #' Create compliance job
    #'
    #' @param create_compliance_job_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateComplianceJobResponse
    #' @export
    create_batch_compliance_job = function(create_compliance_job_request, data_file = NULL, ...) {
      local_var_response <- self$create_batch_compliance_job_with_http_info(create_compliance_job_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create compliance job
    #'
    #' @description
    #' Create compliance job
    #'
    #' @param create_compliance_job_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateComplianceJobResponse) with additional information such as HTTP status code, headers
    #' @export
    create_batch_compliance_job_with_http_info = function(create_compliance_job_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL

      if (missing(`create_compliance_job_request`)) {
        rlang::abort(message = "Missing required parameter `create_compliance_job_request`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `create_compliance_job_request`."))
      }

      if (!missing(`create_compliance_job_request`)) {
        local_var_body <- `create_compliance_job_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/2/compliance/jobs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts = list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types = list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "CreateComplianceJobResponse", loadNamespace("twitter")),
          error = function(e) {
             rlang::abort(message = "Failed to deserialize response",
                          .subclass = "ApiException",
                          ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- paste("Server returned ", local_var_resp$status_code, " response status code.")
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api client exception encountered."
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api server exception encountered."
        }
        rlang::abort(message = error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      }
    },
    #' Get Compliance Job
    #'
    #' @description
    #' Get Compliance Job
    #'
    #' @param id The ID of the Compliance Job to retrieve.
    #' @param compliance_job_fields (optional) A comma separated list of ComplianceJob fields to display.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Get2ComplianceJobsIdResponse
    #' @export
    get_batch_compliance_job = function(id, compliance_job_fields = NULL, data_file = NULL, ...) {
      local_var_response <- self$get_batch_compliance_job_with_http_info(id, compliance_job_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get Compliance Job
    #'
    #' @description
    #' Get Compliance Job
    #'
    #' @param id The ID of the Compliance Job to retrieve.
    #' @param compliance_job_fields (optional) A comma separated list of ComplianceJob fields to display.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Get2ComplianceJobsIdResponse) with additional information such as HTTP status code, headers
    #' @export
    get_batch_compliance_job_with_http_info = function(id, compliance_job_fields = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL

      if (missing(`id`)) {
        rlang::abort(message = "Missing required parameter `id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `id`."))
      }

      query_params["compliance_job.fields"] <- `compliance_job_fields`

      local_var_url_path <- "/2/compliance/jobs/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub(paste0("\\{", "id", "\\}"), URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts = list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types = list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "Get2ComplianceJobsIdResponse", loadNamespace("twitter")),
          error = function(e) {
             rlang::abort(message = "Failed to deserialize response",
                          .subclass = "ApiException",
                          ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- paste("Server returned ", local_var_resp$status_code, " response status code.")
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api client exception encountered."
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api server exception encountered."
        }
        rlang::abort(message = error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      }
    },
    #' Tweets Compliance stream
    #'
    #' @description
    #' Tweets Compliance stream
    #'
    #' @param partition The partition number.
    #' @param backfill_minutes (optional) The number of minutes of backfill requested.
    #' @param start_time (optional) YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the Tweet Compliance events will be provided.
    #' @param end_time (optional) YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp to which the Tweet Compliance events will be provided.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return TweetComplianceStreamResponse
    #' @export
    get_tweets_compliance_stream = function(partition, backfill_minutes = NULL, start_time = NULL, end_time = NULL, data_file = NULL, ...) {
      local_var_response <- self$get_tweets_compliance_stream_with_http_info(partition, backfill_minutes, start_time, end_time, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tweets Compliance stream
    #'
    #' @description
    #' Tweets Compliance stream
    #'
    #' @param partition The partition number.
    #' @param backfill_minutes (optional) The number of minutes of backfill requested.
    #' @param start_time (optional) YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the Tweet Compliance events will be provided.
    #' @param end_time (optional) YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp to which the Tweet Compliance events will be provided.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (TweetComplianceStreamResponse) with additional information such as HTTP status code, headers
    #' @export
    get_tweets_compliance_stream_with_http_info = function(partition, backfill_minutes = NULL, start_time = NULL, end_time = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL

      if (missing(`partition`)) {
        rlang::abort(message = "Missing required parameter `partition`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `partition`."))
      }

      query_params["backfill_minutes"] <- `backfill_minutes`

      query_params["partition"] <- `partition`

      query_params["start_time"] <- `start_time`

      query_params["end_time"] <- `end_time`

      local_var_url_path <- "/2/tweets/compliance/stream"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts = list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types = list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "TweetComplianceStreamResponse", loadNamespace("twitter")),
          error = function(e) {
             rlang::abort(message = "Failed to deserialize response",
                          .subclass = "ApiException",
                          ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- paste("Server returned ", local_var_resp$status_code, " response status code.")
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api client exception encountered."
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api server exception encountered."
        }
        rlang::abort(message = error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      }
    },
    #' Users Compliance stream
    #'
    #' @description
    #' Users Compliance stream
    #'
    #' @param partition The partition number.
    #' @param backfill_minutes (optional) The number of minutes of backfill requested.
    #' @param start_time (optional) YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the User Compliance events will be provided.
    #' @param end_time (optional) YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp from which the User Compliance events will be provided.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return UserComplianceStreamResponse
    #' @export
    get_users_compliance_stream = function(partition, backfill_minutes = NULL, start_time = NULL, end_time = NULL, data_file = NULL, ...) {
      local_var_response <- self$get_users_compliance_stream_with_http_info(partition, backfill_minutes, start_time, end_time, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Users Compliance stream
    #'
    #' @description
    #' Users Compliance stream
    #'
    #' @param partition The partition number.
    #' @param backfill_minutes (optional) The number of minutes of backfill requested.
    #' @param start_time (optional) YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the User Compliance events will be provided.
    #' @param end_time (optional) YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp from which the User Compliance events will be provided.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (UserComplianceStreamResponse) with additional information such as HTTP status code, headers
    #' @export
    get_users_compliance_stream_with_http_info = function(partition, backfill_minutes = NULL, start_time = NULL, end_time = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL

      if (missing(`partition`)) {
        rlang::abort(message = "Missing required parameter `partition`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `partition`."))
      }

      query_params["backfill_minutes"] <- `backfill_minutes`

      query_params["partition"] <- `partition`

      query_params["start_time"] <- `start_time`

      query_params["end_time"] <- `end_time`

      local_var_url_path <- "/2/users/compliance/stream"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts = list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types = list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "UserComplianceStreamResponse", loadNamespace("twitter")),
          error = function(e) {
             rlang::abort(message = "Failed to deserialize response",
                          .subclass = "ApiException",
                          ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- paste("Server returned ", local_var_resp$status_code, " response status code.")
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api client exception encountered."
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api server exception encountered."
        }
        rlang::abort(message = error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      }
    },
    #' List Compliance Jobs
    #'
    #' @description
    #' List Compliance Jobs
    #'
    #' @param type Type of Compliance Job to list.
    #' @param status (optional) Status of Compliance Job to list.
    #' @param compliance_job_fields (optional) A comma separated list of ComplianceJob fields to display.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Get2ComplianceJobsResponse
    #' @export
    list_batch_compliance_jobs = function(type, status = NULL, compliance_job_fields = NULL, data_file = NULL, ...) {
      local_var_response <- self$list_batch_compliance_jobs_with_http_info(type, status, compliance_job_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List Compliance Jobs
    #'
    #' @description
    #' List Compliance Jobs
    #'
    #' @param type Type of Compliance Job to list.
    #' @param status (optional) Status of Compliance Job to list.
    #' @param compliance_job_fields (optional) A comma separated list of ComplianceJob fields to display.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Get2ComplianceJobsResponse) with additional information such as HTTP status code, headers
    #' @export
    list_batch_compliance_jobs_with_http_info = function(type, status = NULL, compliance_job_fields = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL

      if (missing(`type`)) {
        rlang::abort(message = "Missing required parameter `type`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `type`."))
      }

      query_params["type"] <- `type`

      query_params["status"] <- `status`

      query_params["compliance_job.fields"] <- `compliance_job_fields`

      local_var_url_path <- "/2/compliance/jobs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts = list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types = list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "Get2ComplianceJobsResponse", loadNamespace("twitter")),
          error = function(e) {
             rlang::abort(message = "Failed to deserialize response",
                          .subclass = "ApiException",
                          ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- paste("Server returned ", local_var_resp$status_code, " response status code.")
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api client exception encountered."
        }
        rlang::abort(message = local_var_error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        local_var_error_msg <- local_var_resp$response
        if (local_var_error_msg == "") {
          local_var_error_msg <- "Api server exception encountered."
        }
        rlang::abort(message = error_msg,
                     .subclass = "ApiException",
                     ApiException = ApiException$new(http_response = local_var_resp))
      }
    }
  )
)
