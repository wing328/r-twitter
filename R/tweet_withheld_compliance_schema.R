#' Twitter API v2
#'
#' Twitter API v2 available endpoints
#'
#' The version of the OpenAPI document: 2.49
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title TweetWithheldComplianceSchema
#' @description TweetWithheldComplianceSchema Class
#' @format An \code{R6Class} generator object
#' @field withheld  \link{TweetTakedownComplianceSchema}
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TweetWithheldComplianceSchema <- R6::R6Class(
  "TweetWithheldComplianceSchema",
  public = list(
    `withheld` = NULL,
    #' Initialize a new TweetWithheldComplianceSchema class.
    #'
    #' @description
    #' Initialize a new TweetWithheldComplianceSchema class.
    #'
    #' @param withheld withheld
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `withheld`, ...
    ) {
      if (!missing(`withheld`)) {
        stopifnot(R6::is.R6(`withheld`))
        self$`withheld` <- `withheld`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return TweetWithheldComplianceSchema in JSON format
    #' @export
    toJSON = function() {
      TweetWithheldComplianceSchemaObject <- list()
      if (!is.null(self$`withheld`)) {
        TweetWithheldComplianceSchemaObject[["withheld"]] <-
          self$`withheld`$toJSON()
      }

      TweetWithheldComplianceSchemaObject
    },
    #' Deserialize JSON string into an instance of TweetWithheldComplianceSchema
    #'
    #' @description
    #' Deserialize JSON string into an instance of TweetWithheldComplianceSchema
    #'
    #' @param input_json the JSON input
    #' @return the instance of TweetWithheldComplianceSchema
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`withheld`)) {
        withheld_object <- TweetTakedownComplianceSchema$new()
        withheld_object$fromJSON(jsonlite::toJSON(this_object$withheld, auto_unbox = TRUE, digits = NA))
        self$`withheld` <- withheld_object
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return TweetWithheldComplianceSchema in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`withheld`)) {
          sprintf(
          '"withheld":
          %s
          ',
          jsonlite::toJSON(self$`withheld`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of TweetWithheldComplianceSchema
    #'
    #' @description
    #' Deserialize JSON string into an instance of TweetWithheldComplianceSchema
    #'
    #' @param input_json the JSON input
    #' @return the instance of TweetWithheldComplianceSchema
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`withheld` <- TweetTakedownComplianceSchema$new()$fromJSON(jsonlite::toJSON(this_object$withheld, auto_unbox = TRUE, digits = NA))
      self
    },
    #' Validate JSON input with respect to TweetWithheldComplianceSchema
    #'
    #' @description
    #' Validate JSON input with respect to TweetWithheldComplianceSchema and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `withheld`
      if (!is.null(input_json$`withheld`)) {
        stopifnot(R6::is.R6(input_json$`withheld`))
      } else {
        stop(paste("The JSON input `", input, "` is invalid for TweetWithheldComplianceSchema: the required field `withheld` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of TweetWithheldComplianceSchema
    #' @export
    toString = function() {
      self$toJSONString()
    },
    #' Return true if the values in all fields are valid.
    #'
    #' @description
    #' Return true if the values in all fields are valid.
    #'
    #' @return true if the values in all fields are valid.
    #' @export
    isValid = function() {
      # check if the required `withheld` is null
      if (is.null(`withheld`)) {
        FALSE
      }

      TRUE
    },
    #' Return a list of invalid fields (if any).
    #'
    #' @description
    #' Return a list of invalid fields (if any).
    #'
    #' @return A list of invalid fields (if any).
    #' @export
    getInvalidFields = function() {
      invalid_fields <- list()
      # check if the required `withheld` is null
      if (is.null(`withheld`)) {
        invalid_fields[`withheld`] = "Non-nullable required field `withheld` cannot be null."
      }

      invalid_fields
    }
  )
)
